FROM golang:1.23-alpine

# Install air for hot reload, git for cloning repos, tar for build context
RUN apk add --no-cache git tar
RUN go install github.com/air-verse/air@v1.52.3

WORKDIR /app

COPY go.mod go.sum ./
RUN go mod download

COPY . .

EXPOSE 8080

CMD ["air", "-c", ".air.toml"]